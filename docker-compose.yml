version: '3.8'

services:
  resumeforge:
    build: .
    container_name: resumeforge-app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    env_file:
      - .env
    restart: unless-stopped
